// Targeted by JavaCPP version 1.5.3-SNAPSHOT: DO NOT EDIT THIS FILE

package org.bytedeco.cudf;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import static org.bytedeco.cudf.global.cudf.*;
  // namespace detail

/**---------------------------------------------------------------------------*
 * \brief A set of {@code column_view}s of the same size.
 *
 * All public member functions and constructors are inherited from
 *{@code table_view_base<column_view>}.
 *---------------------------------------------------------------------------**/
@Namespace("cudf") @Properties(inherit = org.bytedeco.cudf.presets.cudf.class)
public class table_view extends TableViewBaseColumnView {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public table_view(Pointer p) { super(p); }
    /** Native array allocator. Access with {@link Pointer#position(long)}. */
    public table_view(long size) { super((Pointer)null); allocateArray(size); }
    private native void allocateArray(long size);
    @Override public table_view position(long position) {
        return (table_view)super.position(position);
    }


  public table_view() { super((Pointer)null); allocate(); }
  private native void allocate();

  /**---------------------------------------------------------------------------*
   * \brief Construct a table from a vector of table views
   *
   * \note Because a {@code std::vector} is constructible from a
   * {@code std::initializer_list}, this constructor also supports the following
   * usage:
   * <pre>{@code
   * table_view t0, t1, t2;
   * ...
   * table_view t{{t0,t1,t2}}; // Creates a `table` from the columns of
   * t0, t1, t2
   * }</pre>
   *
   * @throws cudf::logic_error
   * If number of rows mismatch
   *
   * @param views The vector of table views to construct the table from
   *---------------------------------------------------------------------------**/
  public table_view(@StdVector table_view views) { super((Pointer)null); allocate(views); }
  private native void allocate(@StdVector table_view views);

  /**---------------------------------------------------------------------------*
   * \brief Returns a table_view with set of specified columns.
   *
   * @throws std::out_of_range
   * If any element in {@code column_indices} is outside [0, num_columns())
   *
   * @param column_indices Indices of columns in the table
   * @return A table_view consisting of columns from the original table
   * specified by the elements of {@code column_indices}
   *---------------------------------------------------------------------------**/
  public native @ByVal table_view select(@StdVector size_type column_indices);
}
